#!/bin/bash
current_owner=$(stat -c '%U' hello)
if [ "$current_owner" = "guillaume" ]; then
    chown vincent hello
    echo "Owner of 'hello' changed from guillaume to vincent."
else
    echo "Owner of 'hello' is not guillaume. No change made."
fi
